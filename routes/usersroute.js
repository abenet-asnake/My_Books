const express = require('express');
const routes= express.Router();

// all routes defined for users
routes.post('/register',);

routes.post('/login',);

routes.get('/current',);

routes.post('/logout',);

module.exports =routes;